import React from "react";

const LoadingSpinner = () => {
  return (
    <div className="flex justify-center items-center h-screen w-full">
      <button class="mx-auto btn loading">loading</button>
    </div>
  );
};

export default LoadingSpinner;
